// format for declaring a tuple
let user: [string, number, boolean];

// in tuple the order shuld be same the type should be same else you will get a erro
user = ["sultan", 5, true];

let rgb: [number, number, number] = [255, 152, 55];

type newUesr = [string, number];

let AnohterUser: newUesr = ["sultan", 55];

let i = 0;
do {
  console.log(i);
  i++;
} while (i < 10);
